extends base

block content
    h4 Update Video
    form(method="POST")
        input(name="title", placeholder="Video Title", maxlength=80, value=video.title, required)
        input(placeholder="Description", type="text", name="description", minlength=20, value=video.description, required)
        input(placeholder="Hashtags, separated by comma.", required, type="text", name="hashtags", value=video.hashtags.join())
        input(value="Save",type="submit") 