extends ../base
include ../mixins/video


block content
    div.profile__data
        img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each video in videos
            +video(video)
        else
            li Sorry nothing found. 